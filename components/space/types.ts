export type TZoneBoundaries = {
  red: { xMin: number; xMax: number; yMin: number; yMax: number };
  green: { xMin: number; xMax: number; yMin: number; yMax: number };
  orange: { xMin: number; xMax: number; yMin: number; yMax: number };
  blue: { xMin: number; xMax: number; yMin: number; yMax: number };
};
